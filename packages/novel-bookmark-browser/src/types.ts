export type CreateBookmarkButtonProps = {
  /**
   * ボタン押下後に実行する追加処理
   */
  onClick?: () => void;
};

export type updateBookmarkButtonProps = {
  element: HTMLElement;
};

export type UpdateBookmarkListProps = {
  element: HTMLElement;
};
